"""HEARTH — on-device intelligence layer.

A local-first, OpenAI-compatible model gateway for Apple Silicon. See docs/ for the
proposal, architecture, and roadmap. This package is the Phase 0 walking skeleton:
one request goes end-to-end (client -> gateway -> provider -> response).
"""

__version__ = "0.0.1"
